import { ArchitectureDescription } from "./descriptionTypes";
import { Architecture } from "./types";

export function architectureDescriptionToArchitecture(architectureDescription: ArchitectureDescription): Architecture {
    
}